services:
  regcomply:
    build: .
    image: regcomply:latest
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - chroma_db:/app/chroma_db
      - hf_cache:/app/.hf_cache
    restart: unless-stopped

volumes:
  chroma_db:
  hf_cache:
